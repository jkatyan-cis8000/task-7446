# Types Layer

Pure type definitions; no logic, no imports from other internal modules.

This layer defines the data structures and types used throughout the habit tracker:
- Habit definitions
- Completion log entries
- Date/time representations
- Query results
- CSV export formats

No business logic, no side effects, no database access. Only types.
