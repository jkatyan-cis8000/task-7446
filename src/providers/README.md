# Providers Layer

Cross-cutting concerns: auth, telemetry, connectors, feature flags, utilities.

This layer provides:
- Logging and telemetry
- Date/time utilities and timezone handling
- CSV file writing
- Error handling utilities

May import from: types, config, utils, providers
