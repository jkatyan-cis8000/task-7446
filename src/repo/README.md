# Repo Layer

Data access — database operations, file I/O, external state management.

This layer provides:
- SQLite database connection and initialization
- CRUD operations for habits
- Completion log queries
- Data persistence

May import from: types, config, repo
