# UI Layer

User-facing surfaces — CLI commands, input/output, user interactions.

This layer provides:
- CLI command handlers
- User input parsing and validation
- Output formatting and display
- Command routing
- Error message presentation

May import from: types, config, service, runtime, providers, ui
